{
  "name": "checkout-dashboard-example",
  "private": true,
  "type": "module",
  "description": "Example Grafana dashboard built with the Foundation SDK against the bundled observability stack.",
  "scripts": {
    "build": "tsx checkout.ts > checkout.json"
  },
  "dependencies": {
    "@grafana/grafana-foundation-sdk": "^11.6.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
